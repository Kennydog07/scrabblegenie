import React, { useMemo, useRef, useState } from "react";

/*
  Scrabble Solver App — single-file React version
*/

export default function App() {
  return (
    <div style={{padding:"40px",fontFamily:"Arial",background:"#020617",minHeight:"100vh",color:"white"}}>
      <h1>Scrabble Solver App</h1>
      <p>Your full App.jsx code from the canvas should be pasted here.</p>
      <p>This starter package is configured for Vite + React + Tailwind.</p>
    </div>
  );
}
